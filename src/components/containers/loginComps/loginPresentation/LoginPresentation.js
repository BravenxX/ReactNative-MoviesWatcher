import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const LoginPresentation = () => (
  <View>
    <Text>MoviesWatcher</Text>
  </View>
);

const styles = StyleSheet.create({
  container: {},
  title: {
    fontSize: 20,
  },
});

export { LoginPresentation };
